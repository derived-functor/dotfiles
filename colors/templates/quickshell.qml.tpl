import QtQuick

QtObject {
    property color text: "#e0e0e0"
    property color bg: "#${COLOR_BG_RAW}"
    property color bg1: "#${COLOR_SURFACE_RAW}"
    property color bg2: "#${COLOR_OVERLAY_RAW}"
    property color fg: "#${COLOR_FG_RAW}"

    property color red: "#${COLOR_RED_BRIGHT_RAW}"
    property color orange: "#${COLOR_ORANGE_BRIGHT_RAW}"
    property color yellow: "#${COLOR_YELLOW_BRIGHT_RAW}"
    property color green: "#${COLOR_GREEN_BRIGHT_RAW}"
    property color blue: "#${COLOR_BLUE_BRIGHT_RAW}"
    property color purple: "#${COLOR_PURPLE_BRIGHT_RAW}"
    property color aqua: "#${COLOR_AQUA_BRIGHT_RAW}"

    property color accent: "#${COLOR_ACCENT_RAW}"
    property color border: "#${COLOR_BORDER_RAW}"
}
