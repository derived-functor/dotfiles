# For autogen, don't change by hand
$bg       = rgb(${COLOR_BG_RAW})
$bg1      = rgb(${COLOR_SURFACE_RAW})
$bg2      = rgb(${COLOR_OVERLAY_RAW})
$fg       = rgb(${COLOR_FG_RAW})

$red      = rgb(${COLOR_RED_BRIGHT_RAW})
$orange   = rgb(${COLOR_ORANGE_BRIGHT_RAW})
$yellow   = rgb(${COLOR_YELLOW_BRIGHT_RAW})
$green    = rgb(${COLOR_GREEN_BRIGHT_RAW})
$blue     = rgb(${COLOR_BLUE_BRIGHT_RAW})
$purple   = rgb(${COLOR_PURPLE_BRIGHT_RAW})
$aqua     = rgb(${COLOR_AQUA_BRIGHT_RAW})

$accent   = rgb(${COLOR_ACCENT_RAW})
$border   = rgb(${COLOR_BORDER_RAW})

$shadowAlpha = ${COLOR_BG_RAW}
